# Copy this file, replace every goal with the validated fixed Nav2 global-path
# endpoint, then use the copy for all A/B/C/D trials.
results_root: /home/naiwu/fly_brain/gazebo_results
brain_device: cpu
# Required only if controllers includes rewired. Generated with
# rewire_connectome.py; do not point it at original fly-data.
rewired_data_dir: ""
trial_timeout_s: 30.0
startup_grace_s: 90.0
scenarios: [static_front, cross_left, cross_right, front_approach]
controllers: [mppi, heuristic, flybrain, rewired]
seeds: {start: 1000, count: 5}
goals:
  static_front: {frame: map, x: REPLACE_ME, y: REPLACE_ME, yaw: REPLACE_ME}
  cross_left: {frame: map, x: REPLACE_ME, y: REPLACE_ME, yaw: REPLACE_ME}
  cross_right: {frame: map, x: REPLACE_ME, y: REPLACE_ME, yaw: REPLACE_ME}
  front_approach: {frame: map, x: REPLACE_ME, y: REPLACE_ME, yaw: REPLACE_ME}
